# =========================================================================
#  Dependencias del backend — versiones fijadas para los seis integrantes.
#
#  Requiere Python 3.13.x. NO usar 3.14: djangorestframework-simplejwt
#  declara soporte hasta Django 5.2, y Django 5.2 no cubre Python 3.14.
#  El porqué está en docs/entorno/versiones.md.
#
#  Instalación:
#      py -3.13 -m venv .venv
#      .venv\Scripts\activate          (Windows)
#      source .venv/bin/activate       (macOS / Linux)
#      pip install -r requirements.txt
# =========================================================================

# ---------- Núcleo ----------
Django==5.2.17                      # LTS
djangorestframework==3.18.0
djangorestframework-simplejwt==5.5.1

# ---------- Base de datos ----------
# psycopg 3, no psycopg2: es el controlador vigente y trae wheels binarios
# para Windows, macOS y Linux, sin necesidad de compilador.
psycopg[binary]==3.3.4
dj-database-url==3.1.2

# ---------- Configuración ----------
django-environ==0.14.0
django-cors-headers==4.9.0

# ---------- Seguridad ----------
# RNF-04: las contraseñas se almacenan con hash y salt.
argon2-cffi==25.1.0

# ---------- Reportes exportables (caracteristica general 5) ----------
# La consigna pide exportar a Excel, HTML, eMail y PDF. HTML y el correo salen
# de la biblioteca estandar y de Django; los otros dos necesitan estas dos.
#
# Las dos se importan DENTRO de la funcion que las usa, no arriba del modulo:
# asi, un despliegue al que le falte una falla en el unico endpoint que la
# necesita y no al arrancar el proceso. Ver reporting/exporters.py.
openpyxl==3.1.5
# reportlab y no weasyprint: weasyprint necesita GTK/Pango instalados en el
# sistema operativo, que en Windows hay que bajar aparte y en el contenedor de
# Railway obligaria a tocar el constructor. reportlab es Python puro.
reportlab==4.2.5

# ---------- Servir en produccion ----------
# gunicorn NO corre en Windows (necesita fcntl): se instala igual, pero solo
# se ejecuta en el contenedor de Railway, que es Linux. En local se sigue
# usando `manage.py runserver`.
gunicorn==23.0.0
# Sirve los archivos estaticos desde el propio proceso de Django. Sin esto,
# con DEBUG=False el panel de administracion queda sin estilos: Railway no
# tiene un servidor web delante que los sirva.
whitenoise==6.11.0

# ---------- Pruebas ----------
# RNF-08 y criterio 4 de la Definición de Terminado: las pruebas de
# aislamiento se conectan como app_user, nunca como postgres.
pytest==9.1.1
pytest-django==4.14.0
