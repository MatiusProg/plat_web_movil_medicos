[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py
testpaths = tests
addopts = -ra --strict-markers

markers =
    isolation: pruebas de aislamiento multi-inquilino (RNF-08, criterio 4 de la DoD)
